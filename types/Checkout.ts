export type TBillingInfo = {
	firstName: string;
	lastName: string;
	company: string;
	country: string;
	street: string;
	apt: string;
	postcode: string;
	place: string;
	phone: string;
	email: string;
	notes: string;
};

export const TABS = {
	BILLING: "billing",
	PAYMENT: "payment",
};
